// ASSUMPTION: no Brand API documented yet. Structure guessed to
// match the Category pattern. Confirm with backend before relying
// on this beyond local development.
export interface Brand {
    id: number;
    name: string;
  }